source venv/bin/activate
export FLASK_APP=app
flask run --host=0.0.0.0 --port=8080

#run this: ./start.sh to start the server
#run this: sudo service mysql start to start the database
#run this: sudo mysql -u root -p to login
#password for database: root

